name: PWA CI/CD - Staging Environment

on:
  push:
    branches: [staging]
    paths:
      - "src/Mystira.App.PWA/**"
      - "src/Mystira.App.Contracts/**"
      - ".github/workflows/mystira-app-pwa-cicd-staging.yml"
  pull_request:
    branches: [staging]
    types: [opened, synchronize, reopened]
    paths:
      - "src/Mystira.App.PWA/**"
      - "src/Mystira.App.Contracts/**"
      - ".github/workflows/mystira-app-pwa-cicd-staging.yml"
  pull_request:
    branches: [staging]
    types: [closed]
    # No path filter - always cleanup preview environments when PR is closed
  workflow_run:
    workflows: ["Infrastructure Deploy - Staging"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mystira-app-pwa-staging-${{ github.ref }}
  cancel-in-progress: true

env:
  # SWA name follows convention: [org]-[env]-[project]-swa-[region]
  AZURE_SWA_NAME: "mys-staging-mystira-swa-eus2"

jobs:
  build-and-deploy:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.action != 'closed') || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    name: Build and Deploy

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Install wasm-tools workload
        run: dotnet workload install wasm-tools

      - name: Validate API Configuration
        run: |
          echo "Validating appsettings configuration for staging..."

          # Check base config (staging uses the default appsettings.json)
          DEFAULT_API_URL=$(jq -r '.ApiConfiguration.DefaultApiBaseUrl // empty' src/Mystira.App.PWA/wwwroot/appsettings.json)
          if [ -z "$DEFAULT_API_URL" ]; then
            echo "ERROR: Missing DefaultApiBaseUrl in appsettings.json"
            exit 1
          fi

          # Validate URL format
          if ! [[ "$DEFAULT_API_URL" =~ ^https:// ]]; then
            echo "ERROR: API URL must use HTTPS: $DEFAULT_API_URL"
            exit 1
          fi

          # Check that AvailableEndpoints exist and have valid URLs
          ENDPOINTS=$(jq -r '.ApiConfiguration.AvailableEndpoints[]?.Url // empty' src/Mystira.App.PWA/wwwroot/appsettings.json)
          if [ -z "$ENDPOINTS" ]; then
            echo "ERROR: No AvailableEndpoints configured in appsettings.json"
            exit 1
          fi

          # Validate each endpoint URL
          VALIDATION_FAILED=0
          while IFS= read -r url; do
            if [ -n "$url" ] && ! [[ "$url" =~ ^https:// ]]; then
              echo "ERROR: Endpoint URL must use HTTPS: $url"
              VALIDATION_FAILED=1
            fi
          done <<< "$ENDPOINTS"

          if [ "$VALIDATION_FAILED" -eq 1 ]; then
            exit 1
          fi

          echo "✓ API configuration validation passed"
          echo "  - Default API: $DEFAULT_API_URL"
          echo "  - Available endpoints:"
          jq -r '.ApiConfiguration.AvailableEndpoints[] | "    - \(.Name): \(.Url)"' src/Mystira.App.PWA/wwwroot/appsettings.json

      - name: Build Blazor WASM
        run: dotnet publish src/Mystira.App.PWA/Mystira.App.PWA.csproj -c Release -o publish

      - name: Configure Environment Header for Staging
        run: |
          echo "Setting Blazor-Environment header to Staging..."
          jq '.globalHeaders = (.globalHeaders // {}) | .globalHeaders."Blazor-Environment" = "Staging"' publish/wwwroot/staticwebapp.config.json > temp.json
          mv temp.json publish/wwwroot/staticwebapp.config.json
          echo "✓ Blazor-Environment header set to Staging"
          cat publish/wwwroot/staticwebapp.config.json | grep -A 1 "globalHeaders"

      - name: Deploy to Azure Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_STAGING }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "publish/wwwroot"
          skip_app_build: true
          skip_api_build: true

  close-pr:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close PR Preview

    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_STAGING }}
          action: "close"
